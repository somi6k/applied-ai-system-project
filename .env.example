# Copy this file to `.env` and paste your key in. `.env` is gitignored; this
# example file is committed, so never put a real key here.
# Get one (free tier available) at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-key-here
